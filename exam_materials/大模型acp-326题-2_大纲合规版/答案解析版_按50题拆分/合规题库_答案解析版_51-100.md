# 大模型 ACP 答案解析版（第 51-100 题）

## 使用说明

- 本版只保留上一轮按 V3 大纲筛选后的合规题目。
- 每题下方直接给出答案、详细解析、易错点分析和考点，适合考前快速背诵。
- 新题号为合规版连续编号；“原题号”用于回查原始 326 题。

## 试题与答案解析

#### 第 51 题 [单选题]

在 RAG 系统中，将文档拆分成多个分块（chunks）的主要目的是什么？

A. 适配大模型的输入限制，并提高检索的准确性和相关性。
B. 减小存储空间，提高检索效率。
C. 方便用户浏览，提升用户体验。
D. 简化文档预处理流程，降低开发成本。

**答案：`A`**

**考点：** 大模型检索增强 / RAG 核心流程、检索优化、文档解析/切片

**详细解析：**

**相关知识点：** RAG 的关键链路是：文档解析 -> 切片/补上下文 -> embedding 向量化 -> 向量库召回 -> rerank/过滤 -> 拼接上下文生成。答题时先定位问题发生在哪一层。 RAG = 外部知识 + 检索相关片段 + 拼接上下文 + 生成答案。


**原解析补充：**

这是分块的两个核心目的：1.适配模型有限的上下文窗口,2.使检索结果更精准（小分块能更精确匹配查询）

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：适配大模型的输入限制，并提高检索的准确性和相关性。
  - 原因：正确，因为该选项直接改善 RAG 的解析、切片、向量化、召回、重排或上下文质量。
- B 不选：减小存储空间，提高检索效率。
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。
- C 不选：方便用户浏览，提升用户体验。
  - 原因：不选，因为它没有直接命中「RAG 核心流程、检索优化、文档解析/切片」的核心要求，容易把问题带到无关环节。
- D 不选：简化文档预处理流程，降低开发成本。
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。

**易错点分析：**

易错点：不要把所有问题都归因于“大模型不够强”。如果问题出在文档解析、切片、召回或重排，调 temperature 或换生成模型通常不是首选。

**原题号：** 57

---

#### 第 52 题 [单选题]

数据预处理中，将分类任务的标签转化为模型可理解的数字形式称为什么？

A. 文本标准化
B. 词嵌入（Word Embedding）
C. 特征缩放
D. 标签编码（Label Encoding）

**答案：`D`**

**考点：** 大模型微调与数据处理 / 分类标签预处理、标签编码

**详细解析：**

**相关知识点：** 分类任务中的原始标签通常是字符串或业务类别，例如“通过/拒绝”“正向/负向”。训练或评估时，模型和损失函数需要可计算的数值标签，因此要把类别标签映射成数字 ID，这个过程就是标签编码。它解决的是“目标标签如何表示”的问题，不是文本内容如何向量化，也不是数值特征如何缩放。

**原解析补充：**

将类别标签转换为数字，是分类任务的标准预处理步骤。

**解题逻辑：** 题干关键词是“分类任务的标签”和“转化为模型可理解的数字形式”。只要看到“类别 -> 数字编号”，就优先对应 Label Encoding；不要被 Word Embedding 里的“向量/数字表示”带偏，因为词嵌入处理的是文本 token 或词语语义，不是类别标签。

**逐选项原因：**
- A 不选：文本标准化
  - 原因：文本标准化通常是大小写、全半角、符号、空格、繁简或噪声清洗，处理的是输入文本格式，不负责把分类标签映射成数字。
- B 不选：词嵌入（Word Embedding）
  - 原因：词嵌入是把词、句子或文本片段转成稠密向量，用于语义表示；题目问的是分类标签的编码，不是文本语义向量化。
- C 不选：特征缩放
  - 原因：特征缩放用于把连续数值特征调整到相近尺度，例如归一化或标准化；它不用于类别标签到数字 ID 的映射。
- D 正确：标签编码（Label Encoding）
  - 原因：标签编码就是把类别标签转换为模型可处理的数字形式，正好命中题干“分类任务标签转数字”的定义。

**易错点分析：**

易错点：看到“模型可理解的数字形式”容易误选词嵌入。区分方法是看对象：输入文本变向量是 embedding；类别标签变编号是 label encoding；连续数值特征调尺度是 feature scaling。

**原题号：** 58

---
#### 第 53 题 [单选题]

发现机器人输出的内容包含不安全信息，以下哪种做法不推荐？

A. 记录不安全信息，用于后续模型改进和安全策略优化。
B. 直接将原始输出返回给用户。
C. 过滤掉不安全信息，返回剩余安全内容。
D. 向用户返回提示信息，告知其输出内容存在风险。

**答案：`B`**

**考点：** 生产环境应用实践 / 内容安全、合规检查与应用安全

**详细解析：**

**相关知识点：** 生产安全题优先考虑 API Key 安全、输入过滤、输出审核、权限控制、隐私授权、备案、日志追踪和风险兜底。 Production = 安全、合规、部署、性能、成本、监控、降级。


**原解析补充：**

直接返回不安全信息可能造成伦理/法律风险如传播有害内容

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：记录不安全信息，用于后续模型改进和安全策略优化。
  - 原因：不选，因为它会绕开合规、安全或隐私要求，生产环境中风险较高。
- B 正确：直接将原始输出返回给用户。
  - 原因：正确，因为题目问“不能/不推荐/错误”时，该绝对化做法正是风险点。
- C 不选：过滤掉不安全信息，返回剩余安全内容。
  - 原因：不选，因为它会绕开合规、安全或隐私要求，生产环境中风险较高。
- D 不选：向用户返回提示信息，告知其输出内容存在风险。
  - 原因：不选，因为它会绕开合规、安全或隐私要求，生产环境中风险较高。

**易错点分析：**

易错点：“先上线后备案”“直接返回风险内容”“把 Key 写进代码”通常都是错误方向。

**原题号：** 59

---

#### 第 54 题 [单选题]

你需要使用 Qwen-Max 生成一个关于 “Python 基础” 的课程脚本。以下哪种请求方式最合适？

A. 向 Qwen-Max 发送一条包含 “Python 基础课程脚本” 的请求，并附加一些示例代码。
B. 向 Qwen-Max 发送一条详细描述课程目标和内容的请求，例如：“请生成一个 Python 基础课程脚本，包含变量、数据类型、运算符、控制流等内容。”
C. 向 Qwen-Max 发送多条分别包含 “Python 变量”、“Python 数据类型”、“Python 运算符” 等关键词的请求。
D. 向 Qwen-Max 发送一条包含 “Python” 关键词的请求 。

**答案：`B`**

**考点：** 大模型提示词工程 / 提示词框架、系统角色与任务设计

**详细解析：**

**相关知识点：** 提示词工程的核心是把角色、任务、上下文、约束、输出格式、样例写清楚；好的提示词通常具体、可执行、可解析。 Prompt = 把角色、任务、上下文、约束、输出格式和样例讲清楚。


**原解析补充：**

完整性+明确性+效率

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：向 Qwen-Max 发送一条包含 “Python 基础课程脚本” 的请求，并附加一些示例代码。
  - 原因：不选，因为它没有直接命中「提示词框架、系统角色与任务设计」的核心要求，容易把问题带到无关环节。
- B 正确：向 Qwen-Max 发送一条详细描述课程目标和内容的请求，例如：“请生成一个 Python 基础课程脚本，包含变量、数据类型、运算符、控制流等内容。”
  - 原因：正确，因为它与「提示词框架、系统角色与任务设计」的核心判断一致，能直接解决题目描述的问题。
- C 不选：向 Qwen-Max 发送多条分别包含 “Python 变量”、“Python 数据类型”、“Python 运算符” 等关键词的请求。
  - 原因：不选，因为它没有直接命中「提示词框架、系统角色与任务设计」的核心要求，容易把问题带到无关环节。
- D 不选：向 Qwen-Max 发送一条包含 “Python” 关键词的请求 。
  - 原因：不选，因为它没有直接命中「提示词框架、系统角色与任务设计」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：只写“请做好/请准确”不算好提示词。考试更偏向选择具体、结构化、带格式和样例的表达。

**原题号：** 60

---

#### 第 55 题 [单选题]

以下哪个步骤最能体现 RAG 中的 “知识增强” 的思想？

A. 文段召回，从数据库中匹配合适的文段用于回答问题。
B. 语义化分割，基于段落为主体进行语义化分割。
C. 文档解析，将 PDF 转换为 Markdown。
D. 表格单元格改写，kv 形式补充单元格信息，增强语义信息。

**答案：`A`**

**考点：** 大模型检索增强 / RAG 核心流程、检索优化、文档解析/切片

**详细解析：**

**相关知识点：** RAG 的关键链路是：文档解析 -> 切片/补上下文 -> embedding 向量化 -> 向量库召回 -> rerank/过滤 -> 拼接上下文生成。答题时先定位问题发生在哪一层。 RAG = 外部知识 + 检索相关片段 + 拼接上下文 + 生成答案。


**原解析补充：**

查询 → 2. 知识检索 A → 3. 增强生成

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：文段召回，从数据库中匹配合适的文段用于回答问题。
  - 原因：正确，因为该选项直接改善 RAG 的解析、切片、向量化、召回、重排或上下文质量。
- B 不选：语义化分割，基于段落为主体进行语义化分割。
  - 原因：不选，因为它没有直接命中「RAG 核心流程、检索优化、文档解析/切片」的核心要求，容易把问题带到无关环节。
- C 不选：文档解析，将 PDF 转换为 Markdown。
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。
- D 不选：表格单元格改写，kv 形式补充单元格信息，增强语义信息。
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。

**易错点分析：**

易错点：不要把所有问题都归因于“大模型不够强”。如果问题出在文档解析、切片、召回或重排，调 temperature 或换生成模型通常不是首选。

**原题号：** 61

---

#### 第 56 题 [单选题]

以下代码片段中，temperature 参数设置为 0.6，其主要作用是什么？plaintext{"do_sample": true,"temperature": 0.6,"top_p": 0.9,}# other parameters...

A. 控制模型使用的 GPU 数量。
B. 控制模型加载的权重数量。
C. 控制模型生成文本的多样性。
D. 控制模型生成文本的长度。

**答案：`C`**

**考点：** 生产环境应用实践 / 部署发布、性能与生产实践

**详细解析：**

**相关知识点：** 生产实践题重点看延迟、成本、稳定性、降级、缓存、批处理、流式输出、资源限制和工程可维护性。 Production = 安全、合规、部署、性能、成本、监控、降级。


**原解析补充：**

图示引用：ACP 大模型-2.png（原 PDF 文本提取中仅保留到图示引用名）

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：控制模型使用的 GPU 数量。
  - 原因：不选，因为它没有直接命中「部署发布、性能与生产实践」的核心要求，容易把问题带到无关环节。
- B 不选：控制模型加载的权重数量。
  - 原因：不选，因为它没有直接命中「部署发布、性能与生产实践」的核心要求，容易把问题带到无关环节。
- C 正确：控制模型生成文本的多样性。
  - 原因：正确，因为该选项对应生成参数或返回方式，正好解决题目中的随机性、长度、一致性或体验问题。
- D 不选：控制模型生成文本的长度。
  - 原因：不选，因为它没有直接命中「部署发布、性能与生产实践」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：流式输出改善用户等待体验，但不等于降低模型计算成本；缓存/批处理/降级才常用于成本和稳定性优化。

**原题号：** 62

---

#### 第 57 题 [单选题]

在微调大语言模型的数据预处理流程中，哪一步骤旨在减少文本中的噪声，提高数据质量？

A. 序列填充
B. 文本清洗
C. 特征选择
D. 数据增强

**答案：`B`**

**考点：** 大模型微调 / 微调概念、数据构建、训练参数与评测

**详细解析：**

**相关知识点：** 微调是用任务数据继续训练预训练模型，核心关注数据质量、训练循环、损失函数、梯度、学习率、batch size、epoch、验证集和过拟合/欠拟合。 Fine-tuning = 用任务数据更新模型参数，适合稳定任务适配。


**原解析补充：**

微调大语言模型的数据预处理流程中，旨在减少文本中的噪声、提高数据质量的步骤是文本清洗

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：序列填充
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。
- B 正确：文本清洗
  - 原因：正确，因为它与「微调概念、数据构建、训练参数与评测」的核心判断一致，能直接解决题目描述的问题。
- C 不选：特征选择
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。
- D 不选：数据增强
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：微调不是知识库更新的低成本替代；频繁变化的事实知识通常优先用 RAG。训练题要分清 loss、backward、step 的职责。

**原题号：** 63

---

#### 第 58 题 [单选题]

以下代码片段中，哪一个是用于计算模型预测结果（前向传播）的？

A. optimizer.zero_grad()
B. outputs = model(inputs)
C. loss = criterion(outputs, labels)
D. optimizer.step()

**答案：`B`**

**考点：** 大模型微调 / 微调概念、数据构建、训练参数与评测

**详细解析：**

**相关知识点：** 微调是用任务数据继续训练预训练模型，核心关注数据质量、训练循环、损失函数、梯度、学习率、batch size、epoch、验证集和过拟合/欠拟合。 Fine-tuning = 用任务数据更新模型参数，适合稳定任务适配。


**原解析补充：**

B 是前向传播的核心代码，将输入数据 inputs 传递给模型 model，计算并返回预测结果 outputs

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：optimizer.zero_grad()
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。
- B 正确：outputs = model(inputs)
  - 原因：正确，因为它与「微调概念、数据构建、训练参数与评测」的核心判断一致，能直接解决题目描述的问题。
- C 不选：loss = criterion(outputs, labels)
  - 原因：不选，因为它偏训练/微调环节；若题目问的是 RAG 检索、提示词格式或上线合规，就不是最直接答案。
- D 不选：optimizer.step()
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：微调不是知识库更新的低成本替代；频繁变化的事实知识通常优先用 RAG。训练题要分清 loss、backward、step 的职责。

**原题号：** 64

---

#### 第 59 题 [单选题]

在使用 Qwen-Max 辅助内容生成的过程中，以下哪个操作是正确的？

A. 让 Qwen-Max 执行编程代码
B. 利用 Qwen-Max 生成课程脚本
C. 应用 Qwen-Max 进行视频剪辑任务
D. 直接使用 Qwen-Max 进行图像设计

**答案：`B`**

**考点：** 多Agent及多模态应用 / 多模态内容生产与行业应用

**详细解析：**

**相关知识点：** 多模态题要分清文本、图像、语音、视频各自能力边界：TTS 是文本到语音，视觉理解看图片/视频内容，视频编辑负责合成和时间轴。 Agent = 规划和工具调用；多模态 = 跨文本、图像、语音、视频。


**原解析补充：**

Qwen-Max 的核心能力是文本生成，而课程脚本属于其适用场景

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：让 Qwen-Max 执行编程代码
  - 原因：不选，因为它没有直接命中「多模态内容生产与行业应用」的核心要求，容易把问题带到无关环节。
- B 正确：利用 Qwen-Max 生成课程脚本
  - 原因：正确，因为它与「多模态内容生产与行业应用」的核心判断一致，能直接解决题目描述的问题。
- C 不选：应用 Qwen-Max 进行视频剪辑任务
  - 原因：不选，因为它把不同多模态能力混淆了，或没有解决题目所问的内容理解/生成环节。
- D 不选：直接使用 Qwen-Max 进行图像设计
  - 原因：不选，因为它把不同多模态能力混淆了，或没有解决题目所问的内容理解/生成环节。

**易错点分析：**

易错点：不要把 TTS、ASR、视觉理解、视频生成、视频剪辑混为一谈，它们分别解决不同环节。

**原题号：** 65

---

#### 第 60 题 [单选题]

在检测文档内容中是否存在营销性表达时，下列哪种方法可以更全面地识别问题？

A. 识别所有内容错误的部分
B. 让大模型自动修改内容
C. 逐条单独识别营销词汇
D. 一次性生成所有类型的识别结果

**答案：`D`**

**考点：** 大模型应用开发 / 大模型适用场景与应用开发基础

**详细解析：**

**相关知识点：** 大模型适合语义理解、生成、分类、总结、抽取、改写；不适合替代确定性计算、严格统计检验或简单规则能高效完成的任务。 API = 参数、消息角色、流式/批量、对话历史和适用边界。


**原解析补充：**

利用大模型（如 Qwen-Max）的批量分析能力，可同时检测直接营销词汇、隐含诱导性语言、夸张表述等，覆盖更全面。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：识别所有内容错误的部分
  - 原因：不选，因为这种绝对化做法通常忽略必要流程，考试中多为干扰项。
- B 不选：让大模型自动修改内容
  - 原因：不选，因为它没有直接命中「大模型适用场景与应用开发基础」的核心要求，容易把问题带到无关环节。
- C 不选：逐条单独识别营销词汇
  - 原因：不选，因为它没有直接命中「大模型适用场景与应用开发基础」的核心要求，容易把问题带到无关环节。
- D 正确：一次性生成所有类型的识别结果
  - 原因：正确，因为题目问“不能/不推荐/错误”时，该绝对化做法正是风险点。

**易错点分析：**

易错点：大模型不是所有任务最佳工具。确定性计算、统计检验、简单关键词匹配常有更合适的传统方法。

**原题号：** 66

---

#### 第 61 题 [单选题]

以下哪段代码设置了模型训练过程中的学习率？

A. optimizer = torch.optim.Adam(model.parameters(), lr=learning_rate)
B. criterion = nn.CrossEntropyLoss()
C. scheduler = torch.optim.lr_scheduler.StepLR(optimizer, step_size=100, gamma=0.1)
D. learning_rate = 0.001

**答案：`A`**

**考点：** 大模型微调 / 微调概念、数据构建、训练参数与评测

**详细解析：**

**相关知识点：** 微调是用任务数据继续训练预训练模型，核心关注数据质量、训练循环、损失函数、梯度、学习率、batch size、epoch、验证集和过拟合/欠拟合。 Fine-tuning = 用任务数据更新模型参数，适合稳定任务适配。


**原解析补充：**

在优化器（如 Adam）中，lr=learning_rate 直接设置了初始学习率，是训练过程中学习率的基准值。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：optimizer = torch.optim.Adam(model.parameters(), lr=learning_rate)
  - 原因：正确，因为它与「微调概念、数据构建、训练参数与评测」的核心判断一致，能直接解决题目描述的问题。
- B 不选：criterion = nn.CrossEntropyLoss()
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。
- C 不选：scheduler = torch.optim.lr_scheduler.StepLR(optimizer, step_size=100, gamma=0.1)
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。
- D 不选：learning_rate = 0.001
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：微调不是知识库更新的低成本替代；频繁变化的事实知识通常优先用 RAG。训练题要分清 loss、backward、step 的职责。

**原题号：** 67

---

#### 第 62 题 [单选题]

以下哪种方式不符合 RAG 优化的最佳实践？

A. 使用 DashScopeParse 解析文档，提取文本和图片信息。
B. 根据图片内容的复杂程度，选择性地使用视觉模型进行深层次解析。
C. 直接将原始 PDF 文档内容输入向量数据库，不进行任何预处理。
D. 使用语义千问 API 润色目录层级，补全缺失信息。

**答案：`C`**

**考点：** 大模型检索增强 / RAG 核心流程、检索优化、文档解析/切片

**详细解析：**

**相关知识点：** RAG 的关键链路是：文档解析 -> 切片/补上下文 -> embedding 向量化 -> 向量库召回 -> rerank/过滤 -> 拼接上下文生成。答题时先定位问题发生在哪一层。 RAG = 外部知识 + 检索相关片段 + 拼接上下文 + 生成答案。


**原解析补充：**

RAG 的核心是高质量检索，未经处理的原始文档会引入噪声，严重降低效果。其他选项均针对不同环节的优化，属于最佳实践。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：使用 DashScopeParse 解析文档，提取文本和图片信息。
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。
- B 不选：根据图片内容的复杂程度，选择性地使用视觉模型进行深层次解析。
  - 原因：不选，因为它把不同多模态能力混淆了，或没有解决题目所问的内容理解/生成环节。
- C 正确：直接将原始 PDF 文档内容输入向量数据库，不进行任何预处理。
  - 原因：正确，因为该选项直接改善 RAG 的解析、切片、向量化、召回、重排或上下文质量。
- D 不选：使用语义千问 API 润色目录层级，补全缺失信息。
  - 原因：不选，因为它没有直接命中「RAG 核心流程、检索优化、文档解析/切片」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：不要把所有问题都归因于“大模型不够强”。如果问题出在文档解析、切片、召回或重排，调 temperature 或换生成模型通常不是首选。

**原题号：** 68

---

#### 第 63 题 [单选题]

实现早停法（early stopping）时，通常在哪种情况下停止训练？

A. 验证集损失连续 n 轮不再下降
B. 验证集准确率达到 100%
C. 训练集损失达到最小值
D. 训练集准确率连续 n 轮上升

**答案：`A`**

**考点：** 大模型微调 / 微调概念、数据构建、训练参数与评测

**详细解析：**

**相关知识点：** 微调是用任务数据继续训练预训练模型，核心关注数据质量、训练循环、损失函数、梯度、学习率、batch size、epoch、验证集和过拟合/欠拟合。 Fine-tuning = 用任务数据更新模型参数，适合稳定任务适配。


**原解析补充：**

早停法的核心思想是防止模型过拟合，当验证集损失在连续多轮训练中不再下降时，说明模型性能不再提升，此时应停止训练。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：验证集损失连续 n 轮不再下降
  - 原因：正确，因为它与「微调概念、数据构建、训练参数与评测」的核心判断一致，能直接解决题目描述的问题。
- B 不选：验证集准确率达到 100%
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。
- C 不选：训练集损失达到最小值
  - 原因：不选，因为它偏训练/微调环节；若题目问的是 RAG 检索、提示词格式或上线合规，就不是最直接答案。
- D 不选：训练集准确率连续 n 轮上升
  - 原因：不选，因为它偏训练/微调环节；若题目问的是 RAG 检索、提示词格式或上线合规，就不是最直接答案。

**易错点分析：**

易错点：微调不是知识库更新的低成本替代；频繁变化的事实知识通常优先用 RAG。训练题要分清 loss、backward、step 的职责。

**原题号：** 69

---

#### 第 64 题 [单选题]

以下哪个代码片段展示了正确的用户（user）提问方式？

A.
```python
messages = [
{"role": "user", "content": "你是一个诗歌生成器。"},
{"role": "assistant", "content": "我想写一首关于秋天的诗。"}
]
```
B.
```python
messages = [
{"role": "system", "content": "你是一个诗歌生成器。"},
{"role": "assistant", "content": "请告诉我你想写一首关于什么的诗。"}
]
```
C.
```python
messages = [
{"role": "system", "content": "你是一个诗歌生成器。"},
{"role": "user", "content": "我想写一首关于秋天的诗。"}
]
```
D.
```python
messages = [
{"role": "system", "content": "你是一个诗歌生成器。"},
{"role": "system", "content": "我想写一首关于秋天的诗。"}
]
```

**答案：`C`**

**考点：** 大模型应用开发 / API 调用参数、消息角色与对话历史

**详细解析：**

**相关知识点：** API 调用题要分清 model、messages、system/user/assistant、temperature、top_p、stream、seed、max_tokens 等参数各自作用。 API = 参数、消息角色、流式/批量、对话历史和适用边界。


**原解析补充：**

system（可选）→ 设定 AI 身份，user → 用户真实提问，assistant → AI 回复

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：messages = [ {"role": "user", "content": "你是一个诗歌生成器。"}, {"role": "assistant", "content": "我想写一首关于秋天的诗。"} ]
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。
- B 不选：messages = [ {"role": "system", "content": "你是一个诗歌生成器。"}, {"role": "assistant", "content": "请告诉我你想写一首关于什么的诗。...
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。
- C 正确：messages = [ {"role": "system", "content": "你是一个诗歌生成器。"}, {"role": "user", "content": "我想写一首关于秋天的诗。"} ]
  - 原因：正确，因为它让模型获得更清晰的角色、任务、上下文或输出格式约束，能减少歧义。
- D 不选：messages = [ {"role": "system", "content": "你是一个诗歌生成器。"}, {"role": "system", "content": "我想写一首关于秋天的诗。"} ]
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。

**易错点分析：**

易错点：temperature/top_p 控制随机性，max_tokens 控制长度，stream 控制返回方式，seed 控制一致性，system 不是用户提问。

**原题号：** 70

---

#### 第 65 题 [单选题]

以下哪个场景最适合使用规则匹配的文本合规检测？

A. 理解用户提问的深层含义
B. 判断用户评论是否包含特定品牌的广告信息
C. 识别文章中蕴含的复杂情感
D. 检测用户上传的文本是否包含明确的违禁词汇

**答案：`D`**

**考点：** 生产环境应用实践 / 内容安全、合规检查与应用安全

**详细解析：**

**相关知识点：** 生产安全题优先考虑 API Key 安全、输入过滤、输出审核、权限控制、隐私授权、备案、日志追踪和风险兜底。 Production = 安全、合规、部署、性能、成本、监控、降级。


**原解析补充：**

规则匹配的优势场景是明确模式、固定表达的检测如违禁词、格式校验，需要语义理解的场景。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：理解用户提问的深层含义
  - 原因：不选，因为它没有直接命中「内容安全、合规检查与应用安全」的核心要求，容易把问题带到无关环节。
- B 不选：判断用户评论是否包含特定品牌的广告信息
  - 原因：不选，因为它没有直接命中「内容安全、合规检查与应用安全」的核心要求，容易把问题带到无关环节。
- C 不选：识别文章中蕴含的复杂情感
  - 原因：不选，因为它没有直接命中「内容安全、合规检查与应用安全」的核心要求，容易把问题带到无关环节。
- D 正确：检测用户上传的文本是否包含明确的违禁词汇
  - 原因：正确，因为它与「内容安全、合规检查与应用安全」的核心判断一致，能直接解决题目描述的问题。

**易错点分析：**

易错点：“先上线后备案”“直接返回风险内容”“把 Key 写进代码”通常都是错误方向。

**原题号：** 71

---

#### 第 66 题 [单选题]

运行以下代码等待任务结束时，不属于终止状态的是哪一项？

```python
from dashscope import Runs
run = Runs.wait(run_id, thread_id='thread_id')
```

A. completed
B. failed
C. cancelled
D. in_progress

**答案：`D`**

**考点：** 多Agent及多模态应用 / Assistant API、智能体机制与工具调用

**详细解析：**

**相关知识点：** Assistant/Agent 题重点看 Thread、Message、Run、工具函数、函数参数、submit outputs、Planner 路由和任务拆解。 Agent = 规划和工具调用；多模态 = 跨文本、图像、语音、视频。


**原解析补充：**

终止状态指任务不可再变更的最终状态（如完成/失败/取消），而 in_progress 是进行状态，不属于终止状态。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：completed
  - 原因：不选，因为它没有直接命中「Assistant API、智能体机制与工具调用」的核心要求，容易把问题带到无关环节。
- B 不选：failed
  - 原因：不选，因为它没有直接命中「Assistant API、智能体机制与工具调用」的核心要求，容易把问题带到无关环节。
- C 不选：cancelled
  - 原因：不选，因为它没有直接命中「Assistant API、智能体机制与工具调用」的核心要求，容易把问题带到无关环节。
- D 正确：in_progress
  - 原因：正确，因为它与「Assistant API、智能体机制与工具调用」的核心判断一致，能直接解决题目描述的问题。

**易错点分析：**

易错点：工具调用错误通常先查工具描述、参数 schema 和路由逻辑，不要先盲目换大模型。

**原题号：** 72

---

#### 第 67 题 [单选题]

以下哪个步骤不是 RAG 工作流程中 “建立索引” 阶段的一部分？

A. 将用户问题转换为向量表示
B. 将长文本切成更小的片段
C. 将文档解析为纯文本
D. 将文本片段转换为向量表示

**答案：`A`**

**考点：** 大模型检索增强 / RAG 核心流程、检索优化、文档解析/切片

**详细解析：**

**相关知识点：** RAG 的关键链路是：文档解析 -> 切片/补上下文 -> embedding 向量化 -> 向量库召回 -> rerank/过滤 -> 拼接上下文生成。答题时先定位问题发生在哪一层。 RAG = 外部知识 + 检索相关片段 + 拼接上下文 + 生成答案。


**原解析补充：**

建立索引"是离线预处理文档，而选项 A 是在线处理用户请求，属于后续流程

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：将用户问题转换为向量表示
  - 原因：正确，因为该选项直接改善 RAG 的解析、切片、向量化、召回、重排或上下文质量。
- B 不选：将长文本切成更小的片段
  - 原因：不选，因为它没有直接命中「RAG 核心流程、检索优化、文档解析/切片」的核心要求，容易把问题带到无关环节。
- C 不选：将文档解析为纯文本
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。
- D 不选：将文本片段转换为向量表示
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。

**易错点分析：**

易错点：不要把所有问题都归因于“大模型不够强”。如果问题出在文档解析、切片、召回或重排，调 temperature 或换生成模型通常不是首选。

**原题号：** 73

---

#### 第 68 题 [单选题]

为了将不同长度的文本序列调整为统一长度以便模型处理，通常会采取哪种措施？

A. 删除过长的句子
B. 忽略长度差异
C. 截断过短的句子
D. 填充序列到固定长度（Padding）

**答案：`D`**

**考点：** 大模型微调 / 微调概念、数据构建、训练参数与评测

**详细解析：**

**相关知识点：** 微调是用任务数据继续训练预训练模型，核心关注数据质量、训练循环、损失函数、梯度、学习率、batch size、epoch、验证集和过拟合/欠拟合。 Fine-tuning = 用任务数据更新模型参数，适合稳定任务适配。


**原解析补充：**

通过添加特定符号如[PAD]将短序列补长，或截断长序列后填充，确保所有输入长度一致

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：删除过长的句子
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。
- B 不选：忽略长度差异
  - 原因：不选，因为这种绝对化做法通常忽略必要流程，考试中多为干扰项。
- C 不选：截断过短的句子
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。
- D 正确：填充序列到固定长度（Padding）
  - 原因：正确，因为它与「微调概念、数据构建、训练参数与评测」的核心判断一致，能直接解决题目描述的问题。

**易错点分析：**

易错点：微调不是知识库更新的低成本替代；频繁变化的事实知识通常优先用 RAG。训练题要分清 loss、backward、step 的职责。

**原题号：** 74

---

#### 第 69 题 [单选题]

以下哪种分隔符最适合用于区分不同的对话轮次或历史记录？

A. plaintext...=== 用户 ===你好=== 机器人 ===你好，有什么可以帮你？
B. plaintext<chat_history>用户：你好机器人：你好，有什么可以帮你？</chat_history>
C. plaintext##用户你好##机器人你好，有什么可以帮你？
D. plaintext<用户>>你好<机器人>>你好，有什么可以帮你？

**答案：`B`**

**考点：** 大模型提示词工程 / 提示词框架、系统角色与任务设计

**详细解析：**

**相关知识点：** 提示词工程的核心是把角色、任务、上下文、约束、输出格式、样例写清楚；好的提示词通常具体、可执行、可解析。 Prompt = 把角色、任务、上下文、约束、输出格式和样例讲清楚。


**原解析补充：**

机器友好--结构化解析无歧义，人工可读--清晰区分对话角色和内容，标准化符合XML/HTML 等通用数据格式规范。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：plaintext...=== 用户 ===你好=== 机器人 ===你好，有什么可以帮你？
  - 原因：不选，因为它没有直接命中「提示词框架、系统角色与任务设计」的核心要求，容易把问题带到无关环节。
- B 正确：plaintext<chat_history>用户：你好机器人：你好，有什么可以帮你？</chat_history>
  - 原因：正确，因为它与「提示词框架、系统角色与任务设计」的核心判断一致，能直接解决题目描述的问题。
- C 不选：plaintext##用户你好##机器人你好，有什么可以帮你？
  - 原因：不选，因为它没有直接命中「提示词框架、系统角色与任务设计」的核心要求，容易把问题带到无关环节。
- D 不选：plaintext<用户>>你好<机器人>>你好，有什么可以帮你？
  - 原因：不选，因为它没有直接命中「提示词框架、系统角色与任务设计」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：只写“请做好/请准确”不算好提示词。考试更偏向选择具体、结构化、带格式和样例的表达。

**原题号：** 75

---

#### 第 70 题 [单选题]

以下哪个代码片段最能体现大模型的优势？

A.
```python
from scipy.stats import pearsonr
x = [1, 2, 3, 4, 5]
y = [2.4, 6.8, 10]
correlation = pearsonr(x, y)
print(correlation)
```
B.
```python
text = "我非常喜欢这款产品！"
sentiment = analyze_sentiment(text) # 调用大模型接口
print(sentiment) # 输出：积极
```
C.
```python
data = [1, 2, 3, 4, 5]
sum_of_data = sum(data)
print(sum_of_data)
```
D.
```python
import numpy as np
data = [1, 2, 3, 4, 5, 100]
std_dev = np.std(data)
print(std_dev)
```

**答案：`B`**

**考点：** 大模型应用开发 / 大模型适用场景与应用开发基础

**详细解析：**

**相关知识点：** 大模型适合语义理解、生成、分类、总结、抽取、改写；不适合替代确定性计算、严格统计检验或简单规则能高效完成的任务。 API = 参数、消息角色、流式/批量、对话历史和适用边界。


**原解析补充：**

大模型的优势在于理解自然语言和处理复杂语义，情感分析需要理解文本的隐含情绪，这正是大模型的强项。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：from scipy.stats import pearsonr x = [1, 2, 3, 4, 5] y = [2.4, 6.8, 10] correlation = pearsonr(x, y) print(c...
  - 原因：不选，因为它没有直接命中「大模型适用场景与应用开发基础」的核心要求，容易把问题带到无关环节。
- B 正确：text = "我非常喜欢这款产品！" sentiment = analyze_sentiment(text) # 调用大模型接口 print(sentiment) # 输出：积极
  - 原因：正确，因为它属于 Assistant/Agent 的消息、运行或工具调用主流程。
- C 不选：data = [1, 2, 3, 4, 5] sum_of_data = sum(data) print(sum_of_data)
  - 原因：不选，因为它没有直接命中「大模型适用场景与应用开发基础」的核心要求，容易把问题带到无关环节。
- D 不选：import numpy as np data = [1, 2, 3, 4, 5, 100] std_dev = np.std(data) print(std_dev)
  - 原因：不选，因为它没有直接命中「大模型适用场景与应用开发基础」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：大模型不是所有任务最佳工具。确定性计算、统计检验、简单关键词匹配常有更合适的传统方法。

**原题号：** 76

---

#### 第 71 题 [单选题]

以下哪种情况最适合将文档拆分成更小的分块？

A. 文档格式简单，例如纯文本文件。
B. 文档内容包含多个主题，信息量较大。
C. 文档内容非常简短，总共只有几句话。
D. 文档内容高度相关，主题单一 。

**答案：`B`**

**考点：** 大模型提示词工程 / 提示词框架、系统角色与任务设计

**详细解析：**

**相关知识点：** 提示词工程的核心是把角色、任务、上下文、约束、输出格式、样例写清楚；好的提示词通常具体、可执行、可解析。 Prompt = 把角色、任务、上下文、约束、输出格式和样例讲清楚。


**原解析补充：**

分块的核心目的是提高信息检索的精准度。多主题文档拆分后，可确保每块聚焦单一主题，避免检索时返回无关内容

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：文档格式简单，例如纯文本文件。
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。
- B 正确：文档内容包含多个主题，信息量较大。
  - 原因：正确，因为该选项直接改善 RAG 的解析、切片、向量化、召回、重排或上下文质量。
- C 不选：文档内容非常简短，总共只有几句话。
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。
- D 不选：文档内容高度相关，主题单一 。
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。

**易错点分析：**

易错点：只写“请做好/请准确”不算好提示词。考试更偏向选择具体、结构化、带格式和样例的表达。

**原题号：** 77

---

#### 第 72 题 [单选题]

使用模型检查点（checkpointing）保存模型时，一般会保存哪种情况下的模型？

A. 验证集性能最好的模型
B. 训练结束时的模型
C. 每次迭代后的模型
D. 训练初期的模型

**答案：`A`**

**考点：** 大模型微调 / 微调概念、数据构建、训练参数与评测

**详细解析：**

**相关知识点：** 微调是用任务数据继续训练预训练模型，核心关注数据质量、训练循环、损失函数、梯度、学习率、batch size、epoch、验证集和过拟合/欠拟合。 Fine-tuning = 用任务数据更新模型参数，适合稳定任务适配。


**原解析补充：**

模型检查点的标准做法是保存验证集上表现最优的模型，可确保最终选择的是泛化能力最强的版本，而非过拟合的模型。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：验证集性能最好的模型
  - 原因：正确，因为它与「微调概念、数据构建、训练参数与评测」的核心判断一致，能直接解决题目描述的问题。
- B 不选：训练结束时的模型
  - 原因：不选，因为它偏训练/微调环节；若题目问的是 RAG 检索、提示词格式或上线合规，就不是最直接答案。
- C 不选：每次迭代后的模型
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。
- D 不选：训练初期的模型
  - 原因：不选，因为它偏训练/微调环节；若题目问的是 RAG 检索、提示词格式或上线合规，就不是最直接答案。

**易错点分析：**

易错点：微调不是知识库更新的低成本替代；频繁变化的事实知识通常优先用 RAG。训练题要分清 loss、backward、step 的职责。

**原题号：** 78

---

#### 第 73 题 [单选题]

在 RAGAS 的这行代码中，data_samples 可能是什么格式？

```python
dataset = Dataset.from_dict(data_samples)
```

A.
```python
data_samples = {
"question":"sample_question",
"answer":"sample_answer",
"contexts":"sample_contexts"
}
```
B.
```python
data_samples = {
"question":"sample_question",
"answer":"sample_answer",
"contexts":["sample_context1","sample_context2"],
"ground_truth":["sample_ground_truth1","sample_ground_truth2"]
}
```
C.
```python
data_samples = {
"question":"sample_question",
"answer":"sample_answer",
"contexts":["sample_context1","sample_context2"]
}
```
D.
```python
data_samples = DataFrame(
question="sample_question",
answer="sample_answer",
contexts="sample_contexts")
```

**答案：`C`**

**考点：** 大模型检索增强 / RAG 自动化评测与 RAGAS 指标

**详细解析：**

**相关知识点：** RAG 评测要区分检索质量和生成质量：context_recall/context_precision 更关注上下文，faithfulness 关注是否忠于上下文，answer_correctness 关注最终答案正确性。 RAG = 外部知识 + 检索相关片段 + 拼接上下文 + 生成答案。


**原解析补充：**

在 RAGAS（检索增强生成评估框架）中，data_samples 通常需要包含以下核心字段：question: 用户提问（字符串）answer: 模型生成的答案（字符串）contexts: 检索到的参考上下文（列表形式，因 RAG 可能返回多个相关文档片段）

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：data_samples = { "question":"sample_question", "answer":"sample_answer", "contexts":"sample_contexts" }
  - 原因：不选，因为它没有直接命中「RAG 自动化评测与 RAGAS 指标」的核心要求，容易把问题带到无关环节。
- B 不选：data_samples = { "question":"sample_question", "answer":"sample_answer", "contexts":["sample_context1","samp...
  - 原因：不选，因为它没有直接命中「RAG 自动化评测与 RAGAS 指标」的核心要求，容易把问题带到无关环节。
- C 正确：data_samples = { "question":"sample_question", "answer":"sample_answer", "contexts":["sample_context1","samp...
  - 原因：正确，因为它与「RAG 自动化评测与 RAGAS 指标」的核心判断一致，能直接解决题目描述的问题。
- D 不选：data_samples = DataFrame( question="sample_question", answer="sample_answer", contexts="sample_contexts")
  - 原因：不选，因为它没有直接命中「RAG 自动化评测与 RAGAS 指标」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：不要把检索指标和生成指标混用。context 类指标看检索上下文，answer 类指标看最终回答。

**原题号：** 79

---

#### 第 74 题 [单选题]

如果想要自定义 langchain 的 LLM 对象，理论上只需要准备以下哪个要素？

A. 一个 API Key
B. 一个 Model name
C. 一个 JSON，直接加载即可
D. 一个输入为字符串，输出为字符串的函数

**答案：`D`**

**考点：** 生产环境应用实践 / 内容安全、合规检查与应用安全

**详细解析：**

**相关知识点：** 生产安全题优先考虑 API Key 安全、输入过滤、输出审核、权限控制、隐私授权、备案、日志追踪和风险兜底。 Production = 安全、合规、部署、性能、成本、监控、降级。


**原解析补充：**

在 LangChain 中自定义 LLM（大语言模型）对象时，最基础的要求是提供一个能完成文本输入→输出的核心函数

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：一个 API Key
  - 原因：不选，因为它会绕开合规、安全或隐私要求，生产环境中风险较高。
- B 不选：一个 Model name
  - 原因：不选，因为它没有直接命中「内容安全、合规检查与应用安全」的核心要求，容易把问题带到无关环节。
- C 不选：一个 JSON，直接加载即可
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。
- D 正确：一个输入为字符串，输出为字符串的函数
  - 原因：正确，因为它属于 Assistant/Agent 的消息、运行或工具调用主流程。

**易错点分析：**

易错点：“先上线后备案”“直接返回风险内容”“把 Key 写进代码”通常都是错误方向。

**原题号：** 80

---

#### 第 75 题 [单选题]

如果想要发送 “你好，请介绍一下你自己” 给大模型，应该修改以下代码的哪一行？

```python
import os
from openai import OpenAI
client = OpenAI(
api_key=os.getenv("DASHSCOPE_API_KEY"),
base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
)
completion = client.chat.completions.create(
model="qwen-max",
messages=[{"role": "user", "content": "你是谁？"}]
)
print(completion.choices[0].message.content)
```

A. messages=[{"role": "user", "content": "你是谁？"}]
B. api_key=os.getenv("DASHSCOPE_API_KEY")
C. base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
D. model="qwen-max"

**答案：`A`**

**考点：** 生产环境应用实践 / 内容安全、合规检查与应用安全

**详细解析：**

**相关知识点：** 生产安全题优先考虑 API Key 安全、输入过滤、输出审核、权限控制、隐私授权、备案、日志追踪和风险兜底。 Production = 安全、合规、部署、性能、成本、监控、降级。


**原解析补充：**

修改用户提问只需变更 messages 列表中 user 角色的 content 字段，其他配置无需改动

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：messages=[{"role": "user", "content": "你是谁？"}]
  - 原因：正确，因为它让模型获得更清晰的角色、任务、上下文或输出格式约束，能减少歧义。
- B 不选：api_key=os.getenv("DASHSCOPE_API_KEY")
  - 原因：不选，因为它会绕开合规、安全或隐私要求，生产环境中风险较高。
- C 不选：base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
  - 原因：不选，因为它没有直接命中「内容安全、合规检查与应用安全」的核心要求，容易把问题带到无关环节。
- D 不选：model="qwen-max"
  - 原因：不选，因为它没有直接命中「内容安全、合规检查与应用安全」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：“先上线后备案”“直接返回风险内容”“把 Key 写进代码”通常都是错误方向。

**原题号：** 81

---

#### 第 76 题 [单选题]

LlamaIndex 在创建 RAG 应用时，第一步 SimpleDirectoryReader 是将知识文件解析为？

A. Document 对象
B. 元素为 Document 对象的列表
C. 索引 index
D. 包含所有文字信息的字符串

**答案：`B`**

**考点：** 大模型检索增强 / RAG 核心流程、检索优化、文档解析/切片

**详细解析：**

**相关知识点：** RAG 的关键链路是：文档解析 -> 切片/补上下文 -> embedding 向量化 -> 向量库召回 -> rerank/过滤 -> 拼接上下文生成。答题时先定位问题发生在哪一层。 RAG = 外部知识 + 检索相关片段 + 拼接上下文 + 生成答案。


**原解析补充：**

在 LlamaIndex 中，SimpleDirectoryReader 的作用是将知识文件（如 PDF、TXT 等）批量解析为多个 Document 对象组成的列表

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：Document 对象
  - 原因：不选，因为它没有直接命中「RAG 核心流程、检索优化、文档解析/切片」的核心要求，容易把问题带到无关环节。
- B 正确：元素为 Document 对象的列表
  - 原因：正确，因为它与「RAG 核心流程、检索优化、文档解析/切片」的核心判断一致，能直接解决题目描述的问题。
- C 不选：索引 index
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。
- D 不选：包含所有文字信息的字符串
  - 原因：不选，因为这种绝对化做法通常忽略必要流程，考试中多为干扰项。

**易错点分析：**

易错点：不要把所有问题都归因于“大模型不够强”。如果问题出在文档解析、切片、召回或重排，调 temperature 或换生成模型通常不是首选。

**原题号：** 82

---

#### 第 77 题 [单选题]

以下哪个代码片段正确地使用了 system 角色来指示模型扮演一个友好的客服代表？

A.
```python
messages = [
{"role": "system", "content": "你是一位友好的客服代表，随时准备帮助客户解决问题。"}
]
```
B.
```python
messages = [
{"role": "assistant", "content": "您好，请问有什么可以帮您的？"}
]
```
C.
```python
messages = [
{"role": "user", "content": "我想咨询一下退货流程。"}
]
```
D.
```python
messages = [
{"role": "system", "content": "用户想咨询退货流程。"}
]
```

**答案：`A`**

**考点：** 大模型应用开发 / API 调用参数、消息角色与对话历史

**详细解析：**

**相关知识点：** API 调用题要分清 model、messages、system/user/assistant、temperature、top_p、stream、seed、max_tokens 等参数各自作用。 API = 参数、消息角色、流式/批量、对话历史和适用边界。


**原解析补充：**

system 角色用于设定模型的行为和身份

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：messages = [ {"role": "system", "content": "你是一位友好的客服代表，随时准备帮助客户解决问题。"} ]
  - 原因：正确，因为它让模型获得更清晰的角色、任务、上下文或输出格式约束，能减少歧义。
- B 不选：messages = [ {"role": "assistant", "content": "您好，请问有什么可以帮您的？"} ]
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。
- C 不选：messages = [ {"role": "user", "content": "我想咨询一下退货流程。"} ]
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。
- D 不选：messages = [ {"role": "system", "content": "用户想咨询退货流程。"} ]
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。

**易错点分析：**

易错点：temperature/top_p 控制随机性，max_tokens 控制长度，stream 控制返回方式，seed 控制一致性，system 不是用户提问。

**原题号：** 83

---

#### 第 78 题 [单选题]

以下代码片段中，StorageContext 的作用是什么？

```python
storage_context = StorageContext.from_defaults(persist_dir=PERSIST_DIR)
index = load_index_from_storage(storage_context)
```

A. 加载存储在内存中的索引。
B. 创建新的索引。
C. 执行查询操作。
D. 提供加载持久化索引所需的上下文信息。

**答案：`D`**

**考点：** 大模型检索增强 / RAG 核心流程、检索优化、文档解析/切片

**详细解析：**

**相关知识点：** RAG 的关键链路是：文档解析 -> 切片/补上下文 -> embedding 向量化 -> 向量库召回 -> rerank/过滤 -> 拼接上下文生成。答题时先定位问题发生在哪一层。 RAG = 外部知识 + 检索相关片段 + 拼接上下文 + 生成答案。


**原解析补充：**

StorageContext 的作用是提供加载持久化索引所需的上下文信息

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：加载存储在内存中的索引。
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。
- B 不选：创建新的索引。
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。
- C 不选：执行查询操作。
  - 原因：不选，因为它没有直接命中「RAG 核心流程、检索优化、文档解析/切片」的核心要求，容易把问题带到无关环节。
- D 正确：提供加载持久化索引所需的上下文信息。
  - 原因：正确，因为该选项直接改善 RAG 的解析、切片、向量化、召回、重排或上下文质量。

**易错点分析：**

易错点：不要把所有问题都归因于“大模型不够强”。如果问题出在文档解析、切片、召回或重排，调 temperature 或换生成模型通常不是首选。

**原题号：** 84

---

#### 第 79 题 [单选题]

以下哪个代码片段最适合将文本片段转换为向量表示？

A.
```python
from sentence_transformers import SentenceTransformer
model = SentenceTransformer('all-mpnet-base-v2')
def embed_text(text):
return model.encode(text)
```
B.
```python
def embed_text(text):
return len(text)
```
C.
```python
import hashlib
def embed_text(text):
return hashlib.sha256(text.encode()).hexdigest()
```
D.
```python
import numpy as np
def embed_text(text):
return np.random.rand(100) # 随机生成一个 100 维向量
```

**答案：`A`**

**考点：** 大模型应用开发 / 大模型适用场景与应用开发基础

**详细解析：**

**相关知识点：** 大模型适合语义理解、生成、分类、总结、抽取、改写；不适合替代确定性计算、严格统计检验或简单规则能高效完成的任务。 API = 参数、消息角色、流式/批量、对话历史和适用边界。


**原解析补充：**

sentence-transformers 库专为文本嵌入设计，预训练模型（如 all-mpnet-base-v2）生成的向量能捕捉语义信息，适合检索、聚类等任务

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：from sentence_transformers import SentenceTransformer model = SentenceTransformer('all-mpnet-base-v2') def e...
  - 原因：正确，因为它与「大模型适用场景与应用开发基础」的核心判断一致，能直接解决题目描述的问题。
- B 不选：def embed_text(text): return len(text)
  - 原因：不选，因为它没有直接命中「大模型适用场景与应用开发基础」的核心要求，容易把问题带到无关环节。
- C 不选：import hashlib def embed_text(text): return hashlib.sha256(text.encode()).hexdigest()
  - 原因：不选，因为它没有直接命中「大模型适用场景与应用开发基础」的核心要求，容易把问题带到无关环节。
- D 不选：import numpy as np def embed_text(text): return np.random.rand(100) # 随机生成一个 100 维向量
  - 原因：不选，因为它只影响生成随机性、长度或返回方式，不能解决题目真正问的检索、训练、合规或工具调用根因。

**易错点分析：**

易错点：大模型不是所有任务最佳工具。确定性计算、统计检验、简单关键词匹配常有更合适的传统方法。

**原题号：** 85

---

#### 第 80 题 [单选题]

以下哪个提示词示例最有效地利用了分隔符来区分不同的要素？

A. plaintext...## 任务目标写一首关于春天的诗## 受众儿童## 输出格式四行诗
B. plaintext...任务目标是写一首关于春天的诗，受众是儿童，输出格式是四行诗。
C. plaintext...任务目标，写一首关于春天的诗，受众，儿童，输出格式，四行诗
D. plaintext...任务目标写一首关于春天的诗受众儿童输出格式四行诗

**答案：`A`**

**考点：** 大模型提示词工程 / 提示词框架、系统角色与任务设计

**详细解析：**

**相关知识点：** 提示词工程的核心是把角色、任务、上下文、约束、输出格式、样例写清楚；好的提示词通常具体、可执行、可解析。 Prompt = 把角色、任务、上下文、约束、输出格式和样例讲清楚。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：plaintext...## 任务目标写一首关于春天的诗## 受众儿童## 输出格式四行诗
  - 原因：正确，因为它让模型获得更清晰的角色、任务、上下文或输出格式约束，能减少歧义。
- B 不选：plaintext...任务目标是写一首关于春天的诗，受众是儿童，输出格式是四行诗。
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。
- C 不选：plaintext...任务目标，写一首关于春天的诗，受众，儿童，输出格式，四行诗
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。
- D 不选：plaintext...任务目标写一首关于春天的诗受众儿童输出格式四行诗
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。

**易错点分析：**

易错点：只写“请做好/请准确”不算好提示词。考试更偏向选择具体、结构化、带格式和样例的表达。

**原题号：** 86

---

#### 第 81 题 [单选题]

如果让你从 faithfulness,context_recall,context_precision,answer_correctness 中选择一个最能代表 RAG 应用整体性能的指标，你会选哪个？

A. context_precision
B. context_recall
C. answer_correctness
D. faithfulness

**答案：`C`**

**考点：** 大模型检索增强 / RAG 自动化评测与 RAGAS 指标

**详细解析：**

**相关知识点：** RAG 评测要区分检索质量和生成质量：context_recall/context_precision 更关注上下文，faithfulness 关注是否忠于上下文，answer_correctness 关注最终答案正确性。 RAG = 外部知识 + 检索相关片段 + 拼接上下文 + 生成答案。


**原解析补充：**

A/B/D 仅评估局部环节，而 C（answer_correctness） 是唯一端到端衡量 RAG 最终输出质量的指标。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：context_precision
  - 原因：不选，因为它没有直接命中「RAG 自动化评测与 RAGAS 指标」的核心要求，容易把问题带到无关环节。
- B 不选：context_recall
  - 原因：不选，因为它没有直接命中「RAG 自动化评测与 RAGAS 指标」的核心要求，容易把问题带到无关环节。
- C 正确：answer_correctness
  - 原因：正确，因为它与「RAG 自动化评测与 RAGAS 指标」的核心判断一致，能直接解决题目描述的问题。
- D 不选：faithfulness
  - 原因：不选，因为它没有直接命中「RAG 自动化评测与 RAGAS 指标」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：不要把检索指标和生成指标混用。context 类指标看检索上下文，answer 类指标看最终回答。

**原题号：** 87

---

#### 第 82 题 [单选题]

以下代码片段实现了哪种文本合规检查方法？

```python
...
import re
def check_text(text):
pattern = r"假货|劣质|骗子"
if re.search(pattern, text):
return "包含敏感词"
else:
return "文本安全"
text = "这个产品质量很好，不像其他假货"
result = check_text(text)
print(result) # 输出：包含敏感词
...
```

A. 语义分析
B. 模式匹配
C. 机器学习
D. 词性匹配

**答案：`B`**

**考点：** 生产环境应用实践 / 内容安全、合规检查与应用安全

**详细解析：**

**相关知识点：** 生产安全题优先考虑 API Key 安全、输入过滤、输出审核、权限控制、隐私授权、备案、日志追踪和风险兜底。 Production = 安全、合规、部署、性能、成本、监控、降级。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：语义分析
  - 原因：不选，因为它没有直接命中「内容安全、合规检查与应用安全」的核心要求，容易把问题带到无关环节。
- B 正确：模式匹配
  - 原因：正确，因为它与「内容安全、合规检查与应用安全」的核心判断一致，能直接解决题目描述的问题。
- C 不选：机器学习
  - 原因：不选，因为它没有直接命中「内容安全、合规检查与应用安全」的核心要求，容易把问题带到无关环节。
- D 不选：词性匹配
  - 原因：不选，因为它没有直接命中「内容安全、合规检查与应用安全」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：“先上线后备案”“直接返回风险内容”“把 Key 写进代码”通常都是错误方向。

**原题号：** 88

---

#### 第 83 题 [单选题]

def set_default_model():EMBED_MODEL = DashScopeEmbedding(model_name=DashScopeTextEmbeddingsModels.TEXT_EMBEDDING_V2,type_name=DashScopeTextEmbeddingType.TEXT_TYPE_DOCUMENT,)LLM_MODEL = DashScope(model_name="qwen-plus")Settings.embed_model = EMBED_MODELSettings.llm = LLM_MODEL...

A. 使得 llm 可以流式输出
B. 使得 embedding 模型可以参与 llm 的生成过程中
C. 使得有关 LlamaIndex 的后续步骤中无需再次指定 llm 与 embed_model
D. 定义了 EMBED_MODEL 与 LLM_MODEL

**答案：`C`**

**考点：** 大模型检索增强 / RAG 核心流程、检索优化、文档解析/切片

**详细解析：**

**相关知识点：** RAG 的关键链路是：文档解析 -> 切片/补上下文 -> embedding 向量化 -> 向量库召回 -> rerank/过滤 -> 拼接上下文生成。答题时先定位问题发生在哪一层。 RAG = 外部知识 + 检索相关片段 + 拼接上下文 + 生成答案。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：使得 llm 可以流式输出
  - 原因：不选，因为它只影响生成随机性、长度或返回方式，不能解决题目真正问的检索、训练、合规或工具调用根因。
- B 不选：使得 embedding 模型可以参与 llm 的生成过程中
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。
- C 正确：使得有关 LlamaIndex 的后续步骤中无需再次指定 llm 与 embed_model
  - 原因：正确，因为题目问“不能/不推荐/错误”时，该绝对化做法正是风险点。
- D 不选：定义了 EMBED_MODEL 与 LLM_MODEL
  - 原因：不选，因为它没有直接命中「RAG 核心流程、检索优化、文档解析/切片」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：不要把所有问题都归因于“大模型不够强”。如果问题出在文档解析、切片、召回或重排，调 temperature 或换生成模型通常不是首选。

**原题号：** 89

---

#### 第 84 题 [单选题]

以下哪种参数组合会使得模型生成结果最具多样性？

A. temperature = 0.1, top_p = 0.9
B. temperature = 0.1, top_p = 0.1
C. temperature = 0.9, top_p = 0.1
D. temperature = 0.9, top_p = 0.9

**答案：`D`**

**考点：** 大模型应用开发 / API 调用参数、消息角色与对话历史

**详细解析：**

**相关知识点：** API 调用题要分清 model、messages、system/user/assistant、temperature、top_p、stream、seed、max_tokens 等参数各自作用。 API = 参数、消息角色、流式/批量、对话历史和适用边界。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：temperature = 0.1, top_p = 0.9
  - 原因：不选，因为它只影响生成随机性、长度或返回方式，不能解决题目真正问的检索、训练、合规或工具调用根因。
- B 不选：temperature = 0.1, top_p = 0.1
  - 原因：不选，因为它只影响生成随机性、长度或返回方式，不能解决题目真正问的检索、训练、合规或工具调用根因。
- C 不选：temperature = 0.9, top_p = 0.1
  - 原因：不选，因为它只影响生成随机性、长度或返回方式，不能解决题目真正问的检索、训练、合规或工具调用根因。
- D 正确：temperature = 0.9, top_p = 0.9
  - 原因：正确，因为该选项对应生成参数或返回方式，正好解决题目中的随机性、长度、一致性或体验问题。

**易错点分析：**

易错点：temperature/top_p 控制随机性，max_tokens 控制长度，stream 控制返回方式，seed 控制一致性，system 不是用户提问。

**原题号：** 90

---

#### 第 85 题 [单选题]

在文档审阅中，为什么不建议一次输出所有的结果？

A. 只需关注营销表达
B. 大模型可能会漏掉一些错误
C. 内容错误不需要处理
D. 信息量过大，难以理解

**答案：`D`**

**考点：** 大模型提示词工程 / 提示词框架、系统角色与任务设计

**详细解析：**

**相关知识点：** 提示词工程的核心是把角色、任务、上下文、约束、输出格式、样例写清楚；好的提示词通常具体、可执行、可解析。 Prompt = 把角色、任务、上下文、约束、输出格式和样例讲清楚。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：只需关注营销表达
  - 原因：不选，因为它没有直接命中「提示词框架、系统角色与任务设计」的核心要求，容易把问题带到无关环节。
- B 不选：大模型可能会漏掉一些错误
  - 原因：不选，因为它没有直接命中「提示词框架、系统角色与任务设计」的核心要求，容易把问题带到无关环节。
- C 不选：内容错误不需要处理
  - 原因：不选，因为它没有直接命中「提示词框架、系统角色与任务设计」的核心要求，容易把问题带到无关环节。
- D 正确：信息量过大，难以理解
  - 原因：正确，因为它与「提示词框架、系统角色与任务设计」的核心判断一致，能直接解决题目描述的问题。

**易错点分析：**

易错点：只写“请做好/请准确”不算好提示词。考试更偏向选择具体、结构化、带格式和样例的表达。

**原题号：** 91

---

#### 第 86 题 [单选题]

以下哪个代码片段正确地展示了 system、user 和 assistant 三种角色的交互顺序？

A.
```python
messages = [
{"role": "assistant", "content": "你好"},
{"role": "user", "content": "我想订一张机票"},
{"role": "system", "content": "你是一个订票助手"}
]
```
B.
```python
messages = [
{"role": "system", "content": "你是一个订票助手"},
{"role": "user", "content": "我想订一张机票"},
{"role": "assistant", "content": "你好，请问你想去哪里？"}
]
```
C.
```python
messages = [
{"role": "user", "content": "我想订一张机票"},
{"role": "assistant", "content": "你好，请问你想去哪里？"},
{"role": "system", "content": "你是一个订票助手"}
]
```
D.
```python
messages = [
{"role": "user", "content": "我想订一张机票"},
{"role": "system", "content": "你是一个订票助手"},
{"role": "assistant", "content": "你好，请问你想去哪里？"}
```

**答案：`B`**

**考点：** 大模型应用开发 / API 调用参数、消息角色与对话历史

**详细解析：**

**相关知识点：** API 调用题要分清 model、messages、system/user/assistant、temperature、top_p、stream、seed、max_tokens 等参数各自作用。 API = 参数、消息角色、流式/批量、对话历史和适用边界。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：messages = [ {"role": "assistant", "content": "你好"}, {"role": "user", "content": "我想订一张机票"}, {"role": "syste...
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。
- B 正确：messages = [ {"role": "system", "content": "你是一个订票助手"}, {"role": "user", "content": "我想订一张机票"}, {"role": "as...
  - 原因：正确，因为它让模型获得更清晰的角色、任务、上下文或输出格式约束，能减少歧义。
- C 不选：messages = [ {"role": "user", "content": "我想订一张机票"}, {"role": "assistant", "content": "你好，请问你想去哪里？"}, {"role...
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。
- D 不选：messages = [ {"role": "user", "content": "我想订一张机票"}, {"role": "system", "content": "你是一个订票助手"}, {"role": "as...
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。

**易错点分析：**

易错点：temperature/top_p 控制随机性，max_tokens 控制长度，stream 控制返回方式，seed 控制一致性，system 不是用户提问。

**原题号：** 92

---

#### 第 87 题 [单选题]

以下哪个选项最适合用于请求大模型总结一篇文章的主要内容？

A. 用简洁的语言概括以下文章的核心观点：(文章内容)
B. 文章的主要内容是什么？(文章内容)
C. 重写这篇文章。
D. 这篇文章写了什么？

**答案：`A`**

**考点：** 大模型提示词工程 / 提示词框架、系统角色与任务设计

**详细解析：**

**相关知识点：** 提示词工程的核心是把角色、任务、上下文、约束、输出格式、样例写清楚；好的提示词通常具体、可执行、可解析。 Prompt = 把角色、任务、上下文、约束、输出格式和样例讲清楚。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：用简洁的语言概括以下文章的核心观点：(文章内容)
  - 原因：正确，因为它与「提示词框架、系统角色与任务设计」的核心判断一致，能直接解决题目描述的问题。
- B 不选：文章的主要内容是什么？(文章内容)
  - 原因：不选，因为它没有直接命中「提示词框架、系统角色与任务设计」的核心要求，容易把问题带到无关环节。
- C 不选：重写这篇文章。
  - 原因：不选，因为它没有直接命中「提示词框架、系统角色与任务设计」的核心要求，容易把问题带到无关环节。
- D 不选：这篇文章写了什么？
  - 原因：不选，因为它没有直接命中「提示词框架、系统角色与任务设计」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：只写“请做好/请准确”不算好提示词。考试更偏向选择具体、结构化、带格式和样例的表达。

**原题号：** 93

---

#### 第 88 题 [单选题]

在监控模型训练性能时，以下哪段代码可用于计算并打印准确率？

A. loss.backward()
B. optimizer.step()
C. correct /= total; print(f'Accuracy: {100 * correct:.2f}%')
D. running_loss += loss.item() * inputs.size(0)

**答案：`C`**

**考点：** 大模型微调 / 微调概念、数据构建、训练参数与评测

**详细解析：**

**相关知识点：** 微调是用任务数据继续训练预训练模型，核心关注数据质量、训练循环、损失函数、梯度、学习率、batch size、epoch、验证集和过拟合/欠拟合。 Fine-tuning = 用任务数据更新模型参数，适合稳定任务适配。


**原解析补充：**

直接计算并打印准确率的代码片段

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：loss.backward()
  - 原因：不选，因为它偏训练/微调环节；若题目问的是 RAG 检索、提示词格式或上线合规，就不是最直接答案。
- B 不选：optimizer.step()
  - 原因：不选，因为它没有直接命中「微调概念、数据构建、训练参数与评测」的核心要求，容易把问题带到无关环节。
- C 正确：correct /= total; print(f'Accuracy: {100 * correct:.2f}%')
  - 原因：正确，因为它与「微调概念、数据构建、训练参数与评测」的核心判断一致，能直接解决题目描述的问题。
- D 不选：running_loss += loss.item() * inputs.size(0)
  - 原因：不选，因为它偏训练/微调环节；若题目问的是 RAG 检索、提示词格式或上线合规，就不是最直接答案。

**易错点分析：**

易错点：微调不是知识库更新的低成本替代；频繁变化的事实知识通常优先用 RAG。训练题要分清 loss、backward、step 的职责。

**原题号：** 94

---

#### 第 89 题 [单选题]

在 RAGAS 评分之后，你对其中一些指标有些疑问，你可以查看哪个字段来追溯大模型评分的原因？

A. source
B. reason
C. json_object
D. source_node

**答案：`B`**

**考点：** 大模型检索增强 / RAG 自动化评测与 RAGAS 指标

**详细解析：**

**相关知识点：** RAG 评测要区分检索质量和生成质量：context_recall/context_precision 更关注上下文，faithfulness 关注是否忠于上下文，answer_correctness 关注最终答案正确性。 RAG = 外部知识 + 检索相关片段 + 拼接上下文 + 生成答案。


**原解析补充：**

直接通过 reason 字段查看评分依据，其他字段主要用于数据溯源或结构存储

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：source
  - 原因：不选，因为它没有直接命中「RAG 自动化评测与 RAGAS 指标」的核心要求，容易把问题带到无关环节。
- B 正确：reason
  - 原因：正确，因为它与「RAG 自动化评测与 RAGAS 指标」的核心判断一致，能直接解决题目描述的问题。
- C 不选：json_object
  - 原因：不选，因为它没有直接命中「RAG 自动化评测与 RAGAS 指标」的核心要求，容易把问题带到无关环节。
- D 不选：source_node
  - 原因：不选，因为它没有直接命中「RAG 自动化评测与 RAGAS 指标」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：不要把检索指标和生成指标混用。context 类指标看检索上下文，answer 类指标看最终回答。

**原题号：** 95

---

#### 第 90 题 [单选题]

replacements 的作用是：

```python
...
def get_documents(path):
documents = SimpleDirectoryReader(path).load_data()
document = Document(text="
".join([doc.text for doc in documents]))
replacements = {
',': ',',
'.': '.',
'!': '!',
'?': '?'
}
for src, dst in replacements.items():
document.text = document.text.replace(src, dst)
...
return document
```

A. 使得文本更有层级
B. LlamaIndex 按照英文标点切分句子，这么做保证每个句子不会太大
C. 提高大模型对标点符号的识别准确率
D. 降低召回文本段的中文含量

**答案：`B`**

**考点：** 大模型检索增强 / LlamaIndex 文档预处理、文本切分与分块质量

**详细解析：**

**相关知识点：** RAG 或知识库应用在入库前通常要做文档清洗和切分。切分器往往依赖换行、段落或标点判断句子边界；如果中文标点没有被切分器正确识别，就可能形成过长片段，影响 chunk 大小、召回粒度和后续上下文拼接质量。

**解题逻辑：** 这类代码题不要只看变量名，要看代码对 `document.text` 做了什么。`replacements` 被循环用于 `replace`，本质是把某些标点统一成切分器更容易识别的形式，目的是服务后续句子切分和 chunk 控制，而不是提升模型理解能力。

**逐选项原因：**
- A 不选：使得文本更有层级
  - 原因：层级通常来自标题、段落、目录或 metadata 结构化；简单替换标点不会让文本天然产生层级关系。
- B 正确：LlamaIndex 按照英文标点切分句子，这么做保证每个句子不会太大
  - 原因：标点替换是为了让切分器更稳定地识别句子边界，避免中文长句或整段文本被当成一个过大的切分单元，符合代码意图。
- C 不选：提高大模型对标点符号的识别准确率
  - 原因：这段代码发生在文档预处理阶段，目标是影响切分，不是训练或增强大模型对标点的识别能力。
- D 不选：降低召回文本段的中文含量
  - 原因：替换标点不会改变正文语言内容，也不会刻意减少中文文本；召回片段仍然来自原始文档内容。

**易错点分析：**

易错点：代码题容易把“文本替换”理解成清洗噪声。这里关键是结合 LlamaIndex 的切分逻辑看：标点标准化服务于 chunk 边界和检索粒度，不是为了美化文本或改变语义。

**原题号：** 96

---
#### 第 91 题 [单选题]

APIAssistantAgent 示例代码 _forward_and_submit_outputs 函数的核心功能是什么？

```python
...
def _forward_and_submit_outputs(self, run_status):
"""get the function name, run and return the function output to the server"""
func_obj = run_status.required_action.submit_tool_outputs.tool_calls[0].function
func_name = func_obj.name
arguments = json.loads(func_obj.arguments)
tool_outputs = [
{
"tool_call_id":
run_status.required_action.submit_tool_outputs.tool_calls[0].id,
"output": json.dumps(self.funcs[func_name](**arguments))
}
]
run_status = dashscope.Runs.submit_id_outputs(run_status.id,
thread_id=self.thread.id,
tool_outputs=tool_outputs)
...
return
```

A. 执行 function call 并将结果提交给服务器
B. 向 Assistant 提交用户 query
C. 等待 Run 完成
D. 获取 Assistant 的回复
E. 创建新的 Assistant
F. 创建新的 Thread

**答案：`A`**

**考点：** 多Agent及多模态应用 / Assistant API、智能体机制与工具调用

**详细解析：**

**相关知识点：** Assistant/Agent 题重点看 Thread、Message、Run、工具函数、函数参数、submit outputs、Planner 路由和任务拆解。 Agent = 规划和工具调用；多模态 = 跨文本、图像、语音、视频。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：执行 function call 并将结果提交给服务器
  - 原因：正确，因为它与「Assistant API、智能体机制与工具调用」的核心判断一致，能直接解决题目描述的问题。
- B 不选：向 Assistant 提交用户 query
  - 原因：不选，因为它更像本地后处理、无关管理动作或错误的 Agent 调用理解。
- C 不选：等待 Run 完成
  - 原因：不选，因为它更像本地后处理、无关管理动作或错误的 Agent 调用理解。
- D 不选：获取 Assistant 的回复
  - 原因：不选，因为它更像本地后处理、无关管理动作或错误的 Agent 调用理解。
- E 不选：创建新的 Assistant
  - 原因：不选，因为它更像本地后处理、无关管理动作或错误的 Agent 调用理解。
- F 不选：创建新的 Thread
  - 原因：不选，因为它更像本地后处理、无关管理动作或错误的 Agent 调用理解。

**易错点分析：**

易错点：工具调用错误通常先查工具描述、参数 schema 和路由逻辑，不要先盲目换大模型。

**原题号：** 97

---

#### 第 92 题 [单选题]

以下哪种做法不符合利用大模型提升开发效率的思路？

A. 对于所有类型的图片，都使用视觉模型进行深层次解析，以确保信息的完整性。
B. 使用 DashScopeParse 解析文档，而不是自己编写复杂的解析逻辑。
C. 将复杂的文档处理任务拆分成多个子任务，分别使用不同的模型或工具处理。
D. 对于简单的文本替换操作，使用正则表达式而不是调用大模型 API。

**答案：`A`**

**考点：** 多Agent及多模态应用 / 多模态内容生产与行业应用

**详细解析：**

**相关知识点：** 多模态题要分清文本、图像、语音、视频各自能力边界：TTS 是文本到语音，视觉理解看图片/视频内容，视频编辑负责合成和时间轴。 Agent = 规划和工具调用；多模态 = 跨文本、图像、语音、视频。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：对于所有类型的图片，都使用视觉模型进行深层次解析，以确保信息的完整性。
  - 原因：正确，因为它匹配对应的多模态处理环节，能处理文本、语音、图像或视频中的相关信息。
- B 不选：使用 DashScopeParse 解析文档，而不是自己编写复杂的解析逻辑。
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。
- C 不选：将复杂的文档处理任务拆分成多个子任务，分别使用不同的模型或工具处理。
  - 原因：不选，因为它与当前问题的关键环节不匹配，或只解决 RAG 链路中的次要/错误方向。
- D 不选：对于简单的文本替换操作，使用正则表达式而不是调用大模型 API。
  - 原因：不选，因为它更像本地后处理、无关管理动作或错误的 Agent 调用理解。

**易错点分析：**

易错点：不要把 TTS、ASR、视觉理解、视频生成、视频剪辑混为一谈，它们分别解决不同环节。

**原题号：** 98

---

#### 第 93 题 [单选题]

要将索引持久化到磁盘，应使用以下哪个方法？

A. index.store()
B. index.persist()
C. index.save()
D. index.storage_context.persist()

**答案：`D`**

**考点：** 大模型检索增强 / RAG 核心流程、检索优化、文档解析/切片

**详细解析：**

**相关知识点：** RAG 的关键链路是：文档解析 -> 切片/补上下文 -> embedding 向量化 -> 向量库召回 -> rerank/过滤 -> 拼接上下文生成。答题时先定位问题发生在哪一层。 RAG = 外部知识 + 检索相关片段 + 拼接上下文 + 生成答案。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：index.store()
  - 原因：不选，因为它没有直接命中「RAG 核心流程、检索优化、文档解析/切片」的核心要求，容易把问题带到无关环节。
- B 不选：index.persist()
  - 原因：不选，因为它没有直接命中「RAG 核心流程、检索优化、文档解析/切片」的核心要求，容易把问题带到无关环节。
- C 不选：index.save()
  - 原因：不选，因为它没有直接命中「RAG 核心流程、检索优化、文档解析/切片」的核心要求，容易把问题带到无关环节。
- D 正确：index.storage_context.persist()
  - 原因：正确，因为它与「RAG 核心流程、检索优化、文档解析/切片」的核心判断一致，能直接解决题目描述的问题。

**易错点分析：**

易错点：不要把所有问题都归因于“大模型不够强”。如果问题出在文档解析、切片、召回或重排，调 temperature 或换生成模型通常不是首选。

**原题号：** 99

---

#### 第 94 题 [多选题]

已知 prompt_template 定义了让大模型生成摘要的具体要求，以下哪些措施可以提高摘要生成函数的生成质量？

A. 将 document 分成多个段落，分别生成摘要，最后合并。
B. 在 prompt_template 中明确指出需要提取文章的主旨。
C. 不使用 prompt_template，直接调用大模型。
D. 在 prompt_template 中提供一些示例摘要。
E. 在 prompt_template 中提供更详细的输出格式要求，例如指定摘要的长度、语气等。

**答案：`ABDE`**

**考点：** 大模型提示词工程 / 提示词框架、系统角色与任务设计

**详细解析：**

**相关知识点：** 提示词工程的核心是把角色、任务、上下文、约束、输出格式、样例写清楚；好的提示词通常具体、可执行、可解析。 Prompt = 把角色、任务、上下文、约束、输出格式和样例讲清楚。


**原解析补充：**

C 无提示模板会导致输出不可控，质量波动大，完全依赖模型的默认行为，难以满足特定需求。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：将 document 分成多个段落，分别生成摘要，最后合并。
  - 原因：正确，因为它与「提示词框架、系统角色与任务设计」的核心判断一致，能直接解决题目描述的问题。
- B 正确：在 prompt_template 中明确指出需要提取文章的主旨。
  - 原因：正确，因为它让模型获得更清晰的角色、任务、上下文或输出格式约束，能减少歧义。
- C 不选：不使用 prompt_template，直接调用大模型。
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。
- D 正确：在 prompt_template 中提供一些示例摘要。
  - 原因：正确，因为它让模型获得更清晰的角色、任务、上下文或输出格式约束，能减少歧义。
- E 正确：在 prompt_template 中提供更详细的输出格式要求，例如指定摘要的长度、语气等。
  - 原因：正确，因为它让模型获得更清晰的角色、任务、上下文或输出格式约束，能减少歧义。

**易错点分析：**

易错点：只写“请做好/请准确”不算好提示词。考试更偏向选择具体、结构化、带格式和样例的表达。

**原题号：** 100

---

#### 第 95 题 [多选题]

你正在开发一个机器学习领域的答疑机器人，辅助学生学习，但你对该领域专业知识掌握有限，以下哪些策略更有助于你改进提示词设计？

A. 邀请机器学习课程助教一起审核和改进提示词，要求其按照一定的结构来回答问题
B. 要求大模型扮演「机器学习教授」，结合学生认知水平，重新写出更结构化的提示词
C. 按照自己的理解要求大模型按照一定的结构来回答问题
D. 保持提示词简单，避免引入错误的信息

**答案：`AB`**

**考点：** 生产环境应用实践 / 内容安全、合规检查与应用安全

**详细解析：**

**相关知识点：** 生产安全题优先考虑 API Key 安全、输入过滤、输出审核、权限控制、隐私授权、备案、日志追踪和风险兜底。 Production = 安全、合规、部署、性能、成本、监控、降级。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：邀请机器学习课程助教一起审核和改进提示词，要求其按照一定的结构来回答问题
  - 原因：正确，因为它让模型获得更清晰的角色、任务、上下文或输出格式约束，能减少歧义。
- B 正确：要求大模型扮演「机器学习教授」，结合学生认知水平，重新写出更结构化的提示词
  - 原因：正确，因为它让模型获得更清晰的角色、任务、上下文或输出格式约束，能减少歧义。
- C 不选：按照自己的理解要求大模型按照一定的结构来回答问题
  - 原因：不选，因为这种绝对化做法通常忽略必要流程，考试中多为干扰项。
- D 不选：保持提示词简单，避免引入错误的信息
  - 原因：不选，因为它属于提示词的其他要素，或表达不够具体，不能满足题目所问的那个提示词要素。

**易错点分析：**

易错点：“先上线后备案”“直接返回风险内容”“把 Key 写进代码”通常都是错误方向。

**原题号：** 101

---

#### 第 96 题 [多选题]

()关于多模态技术底层原理，正确的说法是？

A. 图像理解模型也需要先将视频或图像分词化再做处理
B. 实现「方言语音合成」需要训练模型建立方言音素与标准发音的映射关系
C. 视频合成所花费的时间与可用的算力呈负相关
D. 文生图模型生成不同尺寸图像时，必须重新训练模型

**答案：`ABC`**

**考点：** 多Agent及多模态应用 / 多模态内容生产与行业应用

**详细解析：**

**相关知识点：** 多模态题要分清文本、图像、语音、视频各自能力边界：TTS 是文本到语音，视觉理解看图片/视频内容，视频编辑负责合成和时间轴。 Agent = 规划和工具调用；多模态 = 跨文本、图像、语音、视频。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：图像理解模型也需要先将视频或图像分词化再做处理
  - 原因：正确，因为它匹配对应的多模态处理环节，能处理文本、语音、图像或视频中的相关信息。
- B 正确：实现「方言语音合成」需要训练模型建立方言音素与标准发音的映射关系
  - 原因：正确，因为该选项直接作用于训练过程、数据质量或模型参数更新，是微调题的核心变量。
- C 正确：视频合成所花费的时间与可用的算力呈负相关
  - 原因：正确，因为它匹配对应的多模态处理环节，能处理文本、语音、图像或视频中的相关信息。
- D 不选：文生图模型生成不同尺寸图像时，必须重新训练模型
  - 原因：不选，因为它偏训练/微调环节；若题目问的是 RAG 检索、提示词格式或上线合规，就不是最直接答案。

**易错点分析：**

易错点：不要把 TTS、ASR、视觉理解、视频生成、视频剪辑混为一谈，它们分别解决不同环节。

**原题号：** 102

---

#### 第 97 题 [多选题]

一家旅游公司计划推出一款沉浸式个性化 AI 导游产品，以下哪些功能设计可以有效提升产品的吸引力和实用性？

A. 使用大语言模型为游客实时生成个性化的景点介绍，结合游客的兴趣和历史游览记录。
B. 开发一个路径规划系统，根据实时交通和天气情况为游客推荐最佳游览路线。
C. 利用图像识别技术分析游客拍摄的照片，自动标注景点名称并提供相关背景信息。
D. 提供一个语音讲解的随身 pad，确保所有游客听到的内容完全相同。

**答案：`ABC`**

**考点：** 多Agent及多模态应用 / 多模态内容生产与行业应用

**详细解析：**

**相关知识点：** 多模态题要分清文本、图像、语音、视频各自能力边界：TTS 是文本到语音，视觉理解看图片/视频内容，视频编辑负责合成和时间轴。 Agent = 规划和工具调用；多模态 = 跨文本、图像、语音、视频。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：使用大语言模型为游客实时生成个性化的景点介绍，结合游客的兴趣和历史游览记录。
  - 原因：正确，因为它与「多模态内容生产与行业应用」的核心判断一致，能直接解决题目描述的问题。
- B 正确：开发一个路径规划系统，根据实时交通和天气情况为游客推荐最佳游览路线。
  - 原因：正确，因为它与「多模态内容生产与行业应用」的核心判断一致，能直接解决题目描述的问题。
- C 正确：利用图像识别技术分析游客拍摄的照片，自动标注景点名称并提供相关背景信息。
  - 原因：正确，因为它匹配对应的多模态处理环节，能处理文本、语音、图像或视频中的相关信息。
- D 不选：提供一个语音讲解的随身 pad，确保所有游客听到的内容完全相同。
  - 原因：不选，因为它把不同多模态能力混淆了，或没有解决题目所问的内容理解/生成环节。

**易错点分析：**

易错点：不要把 TTS、ASR、视觉理解、视频生成、视频剪辑混为一谈，它们分别解决不同环节。

**原题号：** 103

---

#### 第 98 题 [多选题]

某金融智能风控系统在高峰期偶尔出现模型不可用的情况，以下哪些方案可以作为降级策略？

A. 增加 GPU 实例数量
B. 切换至规则引擎兜底
C. 使用备份模型继续服务
D. 关闭部分非核心功能

**答案：`BCD`**

**考点：** 生产环境应用实践 / 部署发布、性能与生产实践

**详细解析：**

**相关知识点：** 生产实践题重点看延迟、成本、稳定性、降级、缓存、批处理、流式输出、资源限制和工程可维护性。 Production = 安全、合规、部署、性能、成本、监控、降级。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：增加 GPU 实例数量
  - 原因：不选，因为它没有直接命中「部署发布、性能与生产实践」的核心要求，容易把问题带到无关环节。
- B 正确：切换至规则引擎兜底
  - 原因：正确，因为它与「部署发布、性能与生产实践」的核心判断一致，能直接解决题目描述的问题。
- C 正确：使用备份模型继续服务
  - 原因：正确，因为它与「部署发布、性能与生产实践」的核心判断一致，能直接解决题目描述的问题。
- D 正确：关闭部分非核心功能
  - 原因：正确，因为它与「部署发布、性能与生产实践」的核心判断一致，能直接解决题目描述的问题。

**易错点分析：**

易错点：流式输出改善用户等待体验，但不等于降低模型计算成本；缓存/批处理/降级才常用于成本和稳定性优化。

**原题号：** 104

---

#### 第 99 题 [多选题]

一家教育科技公司拟开发 AI 教学产品，以下哪些方案能体现 “个性化、智能化、普惠教育”？

A. 自适应学习平台根据学生水平动态调整题库
B. 用 VR 实验室模拟替代昂贵的物理实验，降低教学成本
C. 方言识别系统辅助少数民族学生练习普通话
D. 选择题自动判卷，问答题错别字纠正

**答案：`ABC`**

**考点：** 生产环境应用实践 / 部署发布、性能与生产实践

**详细解析：**

**相关知识点：** 生产实践题重点看延迟、成本、稳定性、降级、缓存、批处理、流式输出、资源限制和工程可维护性。 Production = 安全、合规、部署、性能、成本、监控、降级。

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 正确：自适应学习平台根据学生水平动态调整题库
  - 原因：正确，因为它与「部署发布、性能与生产实践」的核心判断一致，能直接解决题目描述的问题。
- B 正确：用 VR 实验室模拟替代昂贵的物理实验，降低教学成本
  - 原因：正确，因为它与「部署发布、性能与生产实践」的核心判断一致，能直接解决题目描述的问题。
- C 正确：方言识别系统辅助少数民族学生练习普通话
  - 原因：正确，因为它与「部署发布、性能与生产实践」的核心判断一致，能直接解决题目描述的问题。
- D 不选：选择题自动判卷，问答题错别字纠正
  - 原因：不选，因为它没有直接命中「部署发布、性能与生产实践」的核心要求，容易把问题带到无关环节。

**易错点分析：**

易错点：流式输出改善用户等待体验，但不等于降低模型计算成本；缓存/批处理/降级才常用于成本和稳定性优化。

**原题号：** 105

---

#### 第 100 题 [多选题]

某跨境电商平台的客服系统需要处理多语言用户咨询，在进行文本合规检查时发现部分违规内容无法被检测出来。哪两个方案能提供多语言文本的合规检查效果？

A. 强制要求用户使用英语提问，其他语言咨询转入人工处理
B. 建立多语言同义词库，覆盖常见违规表述的变体（如 “免税代购” 的谐音 / 缩写形式）
C. 使用跨语言语义理解模型，识别违规意图而非依赖关键词匹配
D. 将非英语内容翻译为英语后复用单语种审核规则

**答案：`BC`**

**考点：** 生产环境应用实践 / 内容安全、合规检查与应用安全

**详细解析：**

**相关知识点：** 生产安全题优先考虑 API Key 安全、输入过滤、输出审核、权限控制、隐私授权、备案、日志追踪和风险兜底。 Production = 安全、合规、部署、性能、成本、监控、降级。


**原解析补充：**

覆盖变体和语义理解结合能显著提升多语言合规检查效果

**解题逻辑：** 先看题目问的是“最合适/不推荐/主要作用/哪些措施”，再定位到具体环节。单选题通常选最直接解决根因的一项；多选题则保留所有能直接改善该环节的措施，排除只改善无关指标、过度泛化或跳过必要流程的选项。

**逐选项原因：**
- A 不选：强制要求用户使用英语提问，其他语言咨询转入人工处理
  - 原因：不选，因为它没有直接命中「内容安全、合规检查与应用安全」的核心要求，容易把问题带到无关环节。
- B 正确：建立多语言同义词库，覆盖常见违规表述的变体（如 “免税代购” 的谐音 / 缩写形式）
  - 原因：正确，因为它与「内容安全、合规检查与应用安全」的核心判断一致，能直接解决题目描述的问题。
- C 正确：使用跨语言语义理解模型，识别违规意图而非依赖关键词匹配
  - 原因：正确，因为它与「内容安全、合规检查与应用安全」的核心判断一致，能直接解决题目描述的问题。
- D 不选：将非英语内容翻译为英语后复用单语种审核规则
  - 原因：不选，因为它会绕开合规、安全或隐私要求，生产环境中风险较高。

**易错点分析：**

易错点：“先上线后备案”“直接返回风险内容”“把 Key 写进代码”通常都是错误方向。

**原题号：** 106

---
