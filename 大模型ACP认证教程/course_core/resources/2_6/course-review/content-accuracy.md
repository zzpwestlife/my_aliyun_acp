# 内容准确性检查项

## API 一致性
- API 用法是否与最新官方文档一致
- 是否使用了已废弃的 API（如 pandas 的 `append` 方法）
- 参数名和默认值是否与当前版本匹配

## 文本与代码一致性
- 文本描述的操作是否与代码实现一致
  - 例：文本说"用众数填充"，代码是否真的用了 `mode()`
  - 例：文本说"删除重复行"，代码是否真的用了 `drop_duplicates()`
- 代码注释是否与代码行为一致

## 数据与统计
- 引用的数据和统计数字是否有出处
- 示例数据中的数值是否与运行结果一致
- 图表标题和标签是否与数据内容匹配
